import React from "react";
import styled from "styled-components/native";
import { Text } from "react-native";

const HeaderBlock = styled.View``;

const Header = () => {
  return (
    <HeaderBlock>
      <Text>COMMON TODO APP</Text>
    </HeaderBlock>
  );
};

export default Header;
